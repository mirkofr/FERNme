__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.venv/
venv/
# local data + run artifacts (never commit personal data or DBs)
*.db
*.db-*
*.sqlite
*.sqlite3
.fern/
results_*.png
*_state.json
fern.db

# scratch / transcripts / local test files (never commit)

# editor / cloud sync conflict copies (e.g. "__init__ 2.py", "file(1).toml")
* [0-9].*
*([0-9]).*
# scratch / transcripts / local test files
*conversation*.md
*_worktest*

# unpublished manuscript draft (keep local, do not commit)
PAPER.md
